#include <bits/stdc++.h>
using namespace std;
long long a,ans;
int main()
{
    ios_base::sync_with_stdio(false);
    cin.tie(0);
    cout.tie(0);
    cin>>a;
    ans=a*(a-1);
    ans=ans/2;
    cout<<ans;
}
